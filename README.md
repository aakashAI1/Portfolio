# Portfolio (Vercel-ready)

## Run locally

1. Install Node.js (18+ recommended).
2. From `portfolio/`:

```bash
npm install
npm run dev
```

## Customize

- Edit content in `src/content.ts` (projects, links, skills, experience).
- Replace headshot at `src/assets/profile.png`.

